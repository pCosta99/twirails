<div>
  <h1 class="font-bold text-5xl">Timeline</h1>
  <div>
    <ul>
      <li><%= button_to "Users", users_path, method: :get %></li>
      <li><%= button_to "Profile", user_path(@user), method: :get %></li>
      <li><%= button_to "Sign Out", destroy_user_session_path(@user),
                    method: :delete,
                    data: { confirm: "Are you sure?" } %></li>
    </ul>
  </div>

  <div>
    <h2>Add a tweet:</h2>
    <%= render 'tweets/form', user: @user, tweet: @user.tweets.build %>
  </div>

  <div>
    <h2 class="font-bold text-5xl">Tweets</h2>
    <% @all_tweets.each do |tweet| %>
        <%= render tweet %>
    <% end %>
  </div>
</div>
